<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:ViewModel"
             xmlns:views="clr-namespace:View"
             x:Class="View.ColorCapabilityEditor"
             x:DataType="vm:ColorCapabilityViewModel">

  <Border Background="{DynamicResource ListItem-Bg}"
          CornerRadius="6"
          Padding="10 8"
          Margin="0 4">
    <Grid RowDefinitions="Auto,*">
      <TextBlock Grid.Row="0"
                 Text="{Binding Name}"
                 Foreground="{DynamicResource ThemeText2}"
                 FontSize="10"
                 FontWeight="SemiBold"
                 Margin="0 0 0 8" />
      <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="12" HorizontalAlignment="Center">
        <Grid RowDefinitions="*,Auto">
          <views:LevelFader Grid.Row="0" DataContext="{Binding Red}" VerticalAlignment="Stretch" />
          <TextBlock Grid.Row="1" Text="R" Foreground="{DynamicResource ThemeText2}" FontSize="10" HorizontalAlignment="Center" Margin="0 4 0 0" />
        </Grid>
        <Grid RowDefinitions="*,Auto">
          <views:LevelFader Grid.Row="0" DataContext="{Binding Green}" VerticalAlignment="Stretch" />
          <TextBlock Grid.Row="1" Text="G" Foreground="{DynamicResource ThemeText2}" FontSize="10" HorizontalAlignment="Center" Margin="0 4 0 0" />
        </Grid>
        <Grid RowDefinitions="*,Auto">
          <views:LevelFader Grid.Row="0" DataContext="{Binding Blue}" VerticalAlignment="Stretch" />
          <TextBlock Grid.Row="1" Text="B" Foreground="{DynamicResource ThemeText2}" FontSize="10" HorizontalAlignment="Center" Margin="0 4 0 0" />
        </Grid>
      </StackPanel>
    </Grid>
  </Border>

</UserControl>
